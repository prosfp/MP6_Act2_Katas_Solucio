const { plantTree } = require('./kata7');

describe('Kata #7:', () => {

  plantTree("pearTree", "pear"); // { species: 'pearTree', fruit: 'pear' }
  test('should be defined', () => {
    const actual= //El que sigui
    const expected = //El que sigui
    expect(actual).toBe(expected);
  });
});
